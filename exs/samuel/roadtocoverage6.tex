%Multi-synchronisation on events with one-field communications (input and output)
%Syntactic Categories:
%Action ::= Action |[ CSExpr ]| Action (at least 1x) (at least 1 because two branches will synchronise with the GUI, which gives us a third branch...
%Action ::= Comm → Action (at least 2x)
%Comm ::= N CParam (at least 2x)
%CParam ::= ?N (1x)
%CParam ::= !Expr (1x)
%

\begin{circus}
\circchannel a6 : \nat \\
\circprocess Roadtocoverage6 \circdef \circbegin
	\circspot
	    a6?x \then \Skip \lpar | \lchanset a6 \rchanset 
	      | \rpar a6!1 \then \Skip
	      %| \rpar a?y \then \Skip
	    \\
	\circend
\end{circus}
